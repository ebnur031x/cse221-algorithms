# Assignment 1, Problem D — Non-Decreasing Check

Given a list of integers, decide whether it is sorted in non-decreasing
order. Full problem statement (I/O format, constraints, scoring): [`java/_A1D/_A1D_Task.md`](java/_A1D/_A1D_Task.md).

The judge accepts C++, Java, or Python for this task; only the
[Java solution](java/_A1D/Solution.java) has been completed so far. The
`cpp/` and `py/` variants exist locally as unstarted judge boilerplate and
aren't tracked in this repo.
